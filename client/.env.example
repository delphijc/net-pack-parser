# API URL Override
# Leave empty to use relative path (recommended for Docker/Nginx and Vite Proxy)
# VITE_API_URL=http://localhost:3000

# Docker/Nginx handles the proxying of /api requests to the server container.
# Vite handles the proxying of /api requests to localhost:3000 in development.
